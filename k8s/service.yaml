apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      # By default and for convenience, the `targetPort` is set to the same value as the `port` field.
      port: 80 # To external ports
      targetPort: 80
  selector:
    app: nginx-deployment
